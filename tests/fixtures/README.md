# Validation fixtures

Each directory is a complete collection root for one public-validator scenario.
Tests create project roots and real Git submodules in temporary directories when
filesystem or Git metadata is part of the behavior under test.
